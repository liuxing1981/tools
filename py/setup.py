from distutils.core import setup

setup(name="ssh",
      description="ssh run command and upload download file",
      version="1.0",
      author="luis liu",
      author_email="xing.1.liu@nokia-sbell.com",
      packages=['ssh'],
      install_requires=['py2chainmap'])